const $=s=>document.querySelector(s);let source='',target='',policy='skip';
async function api(path,options={}){const r=await fetch(path,{...options,headers:{'Content-Type':'application/json'}});const d=await r.json();if(!r.ok||!d.ok)throw new Error(d.error||`HTTP ${r.status}`);return d;}
async function init(){try{const d=await api('/api/capabilities');$('#lockText').textContent=`simulation_only=${d.simulation_only} · execution_enabled=${d.execution_enabled} · kdialog=${d.selectors.kdialog?'verfügbar':'nicht gefunden'} · zenity=${d.selectors.zenity?'verfügbar':'nicht gefunden'}`;$('#failureMatrix').innerHTML='';d.failure_matrix.forEach(x=>{const p=document.createElement('p');p.textContent=`${x.severity==='red'?'🔴':'🟠'} ${x.id} · ${x.case} → ${x.effect}`;$('#failureMatrix').append(p);});}catch(e){$('#lockText').textContent=`🔴 ${e.message}`;}}
async function select(kind){$('#message').textContent='Auswahldialog wird geöffnet …';try{const d=await api('/api/select',{method:'POST',body:JSON.stringify({kind})});if(d.path){if(kind==='source'){source=d.path;$('#sourcePath').textContent=source;}else{target=d.path;$('#targetPath').textContent=target;}$('#message').textContent='🟢 Auswahl übernommen.';}else $('#message').textContent='🟡 Auswahl abgebrochen.';}catch(e){$('#message').textContent=`🔴 ${e.message}`;}}
$('#sourceBtn').onclick=()=>select('source');$('#targetBtn').onclick=()=>select('target');
$('#policies').onclick=e=>{const b=e.target.closest('[data-policy]');if(!b)return;policy=b.dataset.policy;document.querySelectorAll('[data-policy]').forEach(x=>x.classList.toggle('selected',x===b));};
$('#previewBtn').onclick=async()=>{if(!source||!target){$('#message').textContent='🟡 Bitte zuerst Quelle und Ziel auswählen.';return;}$('#message').textContent='Prüfe nur lesend …';try{const d=await api('/api/preview',{method:'POST',body:JSON.stringify({source,target,conflict_policy:policy})});const p=d.preview;$('#checks').innerHTML='';p.checks.forEach(c=>{const div=document.createElement('div');div.className=`check ${c.status}`;div.textContent=`${c.ok?'🟢':'🔴'} ${c.label}${c.detail?' · '+c.detail:''}`;$('#checks').append(div);});$('#details').textContent=JSON.stringify({simulation_only:p.simulation_only,execution_enabled:p.execution_enabled,mutation_performed:p.mutation_performed,would_copy_if_execution_existed:p.would_copy_if_execution_existed,conflict:p.conflict,recovery_contract:p.recovery_contract},null,2);$('#message').textContent=p.would_copy_if_execution_existed?'🟢 Simulation: Copy wäre nach diesen Prüfungen grundsätzlich möglich. Es wurde nichts verändert.':'🟠 Simulation: Copy wäre aktuell blockiert oder benötigt eine Entscheidung. Es wurde nichts verändert.';}catch(e){$('#message').textContent=`🔴 ${e.message}`;}};
init();